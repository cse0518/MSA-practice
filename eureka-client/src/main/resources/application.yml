spring:
  application:
    name: eureka-client

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka
    disable-delta: true

kafka:
  bootstrapAddress: kafka:9092
topic:
  name: test-topic
